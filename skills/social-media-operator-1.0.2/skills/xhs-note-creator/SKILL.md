---
name: xhs-note-creator
description: "已并入 social-media-operator，不再作为独立技能加载。 / Merged into social-media-operator; no longer loaded as a standalone skill."
version: 2.0.0
---

# 已迁移 / Migrated

小红书文案、卡片渲染和发布能力已并入 `skills/social-media-operator/SKILL.md`。
旧版脚本与素材暂存于本目录，统一入口会逐步迁移并只保留一个渲染实现。
请不要单独加载本目录，避免与统一技能重复触发。

XHS copywriting, card rendering and publishing have been merged into `skills/social-media-operator/SKILL.md`.
Legacy scripts and assets stay in this directory; the unified entry will migrate them and keep a single rendering implementation.
Do not load this directory standalone to avoid duplicate triggers with the unified skill.
